/**
 * casey-lottie.js — hub Lottie disabled (PNG pose stack only).
 * Lottie idle + PNG poses caused double-face rendering on the hub panel.
 * File kept so old script tags 404-safe; no hosts are mounted in templates.
 */
(function initCaseyLottie() {
  'use strict';
  window.CaseyLottie = {
    destroy: function () {},
    remount: function () {},
  };
}());
