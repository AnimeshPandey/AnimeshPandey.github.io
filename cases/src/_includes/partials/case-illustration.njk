{# ── case-illustration.njk — track-themed diagram (synthetic mock) ── #}
<figure class="case-illustration">
  <img class="case-illustration__img"
       src="{{ ('/assets/case-illustrations/' ~ track ~ '.svg') | url }}"
       alt=""
       width="640"
       height="200"
       loading="lazy"
       decoding="async" />
  <figcaption class="case-illustration__caption">
    <span class="tone-junior">Annotated pattern — not a real product screenshot.</span>
    <span class="tone-mid">Synthetic layout illustrating the broken vs fixed shape of the UI.</span>
    <span class="tone-staff">Casebook mock only; cite production examples from the references chapter.</span>
  </figcaption>
</figure>
