{
  "name": "the-frontend-casebook",
  "version": "1.0.0",
  "description": "The Frontend Casebook — Eleventy 11 static site, pathPrefix /cases/",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve --port 8080",
    "debug": "DEBUG=Eleventy* eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
